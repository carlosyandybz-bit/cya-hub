import { expect, test, type Page } from "@playwright/test";
async function openAgenda(page:Page){const email=process.env.QA_TEACHER_EMAIL,password=process.env.QA_TEACHER_PASSWORD;test.skip(!email||!password,"teacher QA credentials are not configured");await page.goto("/",{waitUntil:"domcontentloaded"});await page.locator('input[name="email"]').fill(email!);await page.locator('input[name="password"]').fill(password!);await page.getByRole("button",{name:/^Entrar$/}).click();await expect(page.locator('input[name="email"]')).toBeHidden({timeout:20000});await page.getByRole("button",{name:/Agenda completa/i}).click();await expect(page.getByRole("heading",{name:/Calendario CYA/i})).toBeVisible({timeout:20000});}
for(const width of [390,430,1280] as const){test(`P36 agenda month is touch-safe without horizontal scrolling at ${width}px`,async({page})=>{await page.setViewportSize({width,height:width<1000?844:800});await openAgenda(page);await page.getByRole("button",{name:/^Mes$/}).click();await expect(page.locator(".month-calendar")).toBeVisible();const geometry=await page.evaluate(()=>{const calendar=document.querySelector<HTMLElement>(".month-calendar"),days=document.querySelector<HTMLElement>(".month-days");return{documentOverflow:document.documentElement.scrollWidth-document.documentElement.clientWidth,calendarOverflow:calendar?calendar.scrollWidth-calendar.clientWidth:0,daysOverflow:days?days.scrollWidth-days.clientWidth:0};});expect(geometry.documentOverflow).toBeLessThanOrEqual(1);expect(geometry.calendarOverflow).toBeLessThanOrEqual(1);expect(geometry.daysOverflow).toBeLessThanOrEqual(1);const controls=page.locator(".calendar-toolbar button:visible, .calendar-filters button:visible");if(width<1000){const small=await controls.evaluateAll(buttons=>buttons.filter(button=>{const rect=button.getBoundingClientRect();return rect.width<44||rect.height<44;}).length);expect(small).toBe(0);}});}