name: Validate Drive folder hardening

on:
  push:
    branches:
      - fix/drive-file-folder-clean
    paths:
      - .github/workflows/validate-drive-file-clean.yml

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/drive-file-folder-clean
      - uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: node --test tests/rendered-html.test.mjs
