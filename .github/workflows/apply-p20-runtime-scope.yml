name: Retired P20 runtime scope helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P20 runtime scope is already persisted; no mutation is performed.'
