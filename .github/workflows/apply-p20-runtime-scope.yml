name: Retired P20 dry-run correction helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P20 dry-run correction is fully persisted; this workflow performs no mutation.'
